{{#paper-toolbar}}
   {{#paper-toolbar-tools}}
    {{#paper-sidenav-toggle name="left2" as |toggleAction|}}
            {{#paper-button classNames="hide-gt-sm" iconButton=true onClick=(action toggleAction)}}
      {{paper-icon icon="menu"}}
      {{/paper-button}}
    {{/paper-sidenav-toggle}}
     <h2>Verwaltung {{paper-icon "chevron right"}} Allgemein</h2>
     <span class="flex"></span>
   {{/paper-toolbar-tools}}
 {{/paper-toolbar}}

{{#paper-tabs selected=currentRouteName primary=true borderBottom=true as |tabs|}}
  {{#tabs.tab value="management.general.index" onClick=(transition-to 'management.general.index')}}
    Semester
  {{/tabs.tab}}
  {{#tabs.tab value="management.general.faculties" onClick=(transition-to 'management.general.faculties')}}
    Fakultäten
  {{/tabs.tab}}
  {{#tabs.tab value="management.general.coursetypes" onClick=(transition-to 'management.general.coursetypes')}}
    Veranstaltungstypen
  {{/tabs.tab}}
{{/paper-tabs}}

{{liquid-outlet class="md-padding overflow-y-scroll"}}
