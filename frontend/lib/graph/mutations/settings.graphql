mutation updateSetting($setting: NewSetting!) {
    updateSetting(setting: $setting) {
        value
    }
}

mutation updateAboutSectionText($text: String!) {
    updateAboutSectionText(text: $text)
}