query login ($mail: String!, $password: String!) {
    login(mail: $mail, password: $password)
}

query singleUser ($mail: String!) {
    users(mail: [$mail]) {
        firstname,
        lastname,
        mail,
        role
    }
}