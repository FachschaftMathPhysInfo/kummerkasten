query allSettings {
    settings {
        key
        value
    }
}

query footerSettings {
    footerSettings {
        key
        value
    }
}

query aboutSectionSettings {
    aboutSectionSettings {
        key
        value
    }
}