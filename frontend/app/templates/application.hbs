{{#paper-sidenav-container class="layout-row layout-xs-column"}}
{{#paper-sidenav
  class="md-whiteframe-z2 site-sidenav flex overflow-x-hidden"
  name="left2"
  open=open
  closeOnClick=true
  onToggle=(action (mut open))
flex-layout="column" flex=true }}
{{#paper-toolbar tall=true as |toolbar|}}
  {{#paper-toolbar-tools}}
  <div style="margin:auto; text-align:center; padding: 20px">
    <div style="width:140px; height:140px;position:relative">
    {{svg-jar "face" style="position:absolute; top:0px; left:0px;"}}
    {{svg-jar "tear2" style="position:absolute; top:0px; right:0px; width:28%; height:28%;" id="tear"}}
  </div>
    <br/>KUMMERKASTEN
  </div>{{/paper-toolbar-tools}}
{{/paper-toolbar}}
  {{#paper-content padding=false}}
    {{#paper-list}}
      {{#menu-item active=(is-active "index" currentRouteName) onClick=(transition-to "index")}}{{paper-icon "home" class="site-nav-icon"}}Startseite{{/menu-item}}
      {{#if session.isAuthenticated}}{{#menu-item onClick=(transition-to "inbox") active=(is-active "inbox" currentRouteName)}}{{paper-icon "toc" class="site-nav-icon"}}Übersicht{{/menu-item}}{{/if}}


    {{/paper-list}}
  {{/paper-content}}
{{/paper-sidenav}}

  {{#paper-content flex-layout="column" class="flex row layout-column overflow-x-hidden"}}
    {{outlet}}
     {{paper-toaster}}
    {{/paper-content}}

 {{/paper-sidenav-container}}
